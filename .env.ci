# .env.ci — Default value for GitHub Actions CI

SPRING_PROFILES_ACTIVE=test
SPRING_JPA_HIBERNATE_DDL_AUTO=create-drop
SPRING_FLYWAY_ENABLED=false

MAIL_HOST=localhost
MAIL_PORT=3025
MAIL_FROM=ci@aries.local

EXPORT_DIR=/tmp/aries-ci-exports
EXPORT_TTL_HOURS=1
CACHE_TTL_MINUTES=1
SERVER_PORT=8081
